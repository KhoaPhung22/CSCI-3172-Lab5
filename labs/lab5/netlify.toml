[build]
  command = ""
  publish = "public"
  functions = "netlify/functions"
  
  [build.environment]
    SECRETS_SCAN_OMIT_KEYS = "PORT"

[[redirects]]
  from = "/api/*"
  to   = "/.netlify/functions/api/:splat"
  status = 200
